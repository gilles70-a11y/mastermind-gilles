name: Build Android APK Finale
on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java e Android SDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Bubblewrap
        run: npm install -g @bubblewrap/cli

      - name: Generazione Progetto
        run: |
          mkdir -p ~/.bubblewrap
          echo "{\"jdkPath\":\"$JAVA_HOME\",\"androidSdkPath\":\"$ANDROID_HOME\"}" > ~/.bubblewrap/config.json
          
          # URL completi e validi per evitare l'errore "Invalid URL"
          echo '{
            "packageId": "com.mastermind.gilles",
            "host": "gilles70-a11y.github.io",
            "name": "Mastermind",
            "launcherName": "Mastermind",
            "display": "standalone",
            "themeColor": "#000000",
            "navigationColor": "#000000",
            "backgroundColor": "#000000",
            "startUrl": "https://gilles70-a11y.github.io/mastermind-pwa/",
            "iconUrl": "https://via.placeholder.com/512.png",
            "maskableIconUrl": "https://via.placeholder.com/512.png",
            "webManifestUrl": "https://gilles70-a11y.github.io/mastermind-pwa/manifest.json",
            "fallbackType": "customtabs",
            "features": { "locationDelegation": false, "playBilling": false },
            "alphaCode": 1,
            "minSdkVersion": 19,
            "targetSdkVersion": 34,
            "enableNotification": true,
            "orientation": "any",
            "appVersionName": "1.0.0",
            "appVersionCode": 1,
            "signingKey": { "path": "./android.keystore", "alias": "android" },
            "generatorApp": "bubblewrap-cli",
            "signingMode": "none"
          }' > twa-manifest.json

          # Inizializziamo senza chiedere nulla all'utente
          yes | bubblewrap init --manifest twa-manifest.json --skipConfig || true

      - name: Compilazione Forzata
        run: |
          # Cerchiamo il file gradlew ovunque sia stato creato
          GRADLE_BIN=$(find . -name gradlew | head -n 1)
          if [ -z "$GRADLE_BIN" ]; then
            echo "ERRORE: Sistema di build non trovato."
            exit 1
          fi
          chmod +x "$GRADLE_BIN"
          cd $(dirname "$GRADLE_BIN")
          ./gradlew assembleDebug

      - name: Carica APK
        uses: actions/upload-artifact@v4
        with:
          name: Mastermind-APK
          # Cerchiamo ricorsivamente qualsiasi APK generato
          path: "**/*.apk"
          if-no-files-found: error
          
